// The strip above the terminal: model, effort, context, /compact, /clear, past conversations.
// Stub: the real bar lands with the session work — plan §3.5. Owner: B.

import type { SessionState, Workspace } from '../store'

export function SessionBar(_props: { ws: Workspace | null; session: SessionState | null }) {
  return null
}
