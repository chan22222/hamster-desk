# electron-builder lists optional peers (Squirrel installer tooling) this app never uses. Without this,
# a plain "npm install" adds 13 packages to package-lock.json, and that drift then rides along into
# a commit or blocks a pull. With it, "npm install" and "npm install --legacy-peer-deps" are the same.
legacy-peer-deps=true
