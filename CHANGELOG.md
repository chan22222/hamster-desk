# 업데이트 내역

버전마다 사용자가 체감하는 변화를 적는다. 설치한 앱은 GitHub Releases 의 버전으로 새 버전을 판단하고, 릴리스 노트는 이 파일의 해당 버전 절이다(`scripts/release.ts`).

## 0.1.21 — 2026-09-23

- **오래 쓸수록 느려지던 것이 풀렸다.** 사이드바의 `말풍선 로그`와 `바뀐 파일`은 세션마다 500줄까지 쌓이는데, 활성 세션에 무슨 일이 생길 때마다 그 목록을 통째로 다시 그렸다. 로그가 차면 한 번에 24–36ms 가 들었고, 그 스레드는 터미널이 글자를 그리는 스레드라 타이핑까지 버벅였다. 이제 보이는 줄만 그리고 바뀐 줄만 다시 그려서 4–5ms 이고, 로그 길이와 상관없다. 두 목록은 `↑` `↓` `Home` `End` 로 줄을 옮길 수 있고, 펼친 diff 는 멀리 스크롤했다 돌아와도 다시 불러오지 않는다.
- **터미널 맨 아랫줄이 잘리지 않는다.** 줄 수를 셀 때 터미널 위쪽 여백과 테두리(7px)까지 빈자리로 쳐서, 창 높이에 따라(열에 네 번꼴) 들어가지 않는 줄이 하나 더 생겨 Claude Code 의 맨 아랫줄이 잘렸다. 글자가 오른쪽 스크롤바 밑으로 들어가던 것과, 배율이 다른 모니터로 창을 옮겼을 때 줄 수를 다시 맞추지 않던 것도 함께 고쳤다.
- **중단되거나 실패한 서브에이전트, 워크플로 에이전트도 퇴근한다.** "끝났다"는 표시를 남기지 않는 이런 에이전트가 세션이 끝날 때까지 일하는 모습으로 자리를 지켰다. 앱을 켜거나 `--resume` 하면 지난 에이전트가 한꺼번에 되살아나기도 했다(워크플로를 여러 번 돌린 세션에서 100마리 넘게). 이제 부모 대화에 오는 완료 알림과 워크플로의 결과 제출로도 끝난 것을 알아본다.
- **세션이 잠깐 사라졌다 돌아오지 않는다.** Claude Code 가 세션 파일을 고쳐 쓰는 순간에 읽으면 살아 있는 세션을 끝났다고 보고 곧바로 다시 붙이면서, 책상이 비었다 다시 차고 대화 전체를 다시 읽었다.
- **작업 알림이 메인 햄스터의 말풍선을 지우지 않는다.** 서브에이전트가 끝났다는 알림, 슬래시 명령, `!` 셸, 중단 표시가 사람이 친 프롬프트로 잡혀서 올 때마다 말풍선이 비워지고 마지막 프롬프트가 알림 글로 바뀌었다.

## 0.1.20 — 2026-09-23

- **스튜디오가 다시 부드럽다.** 0.1.19 는 핵폭발·걷기 같은 움직임도 60fps 로 묶어서, 165Hz 같은 고주사율 모니터에서는 전보다 끊겨 보였다(화면 갱신 세 번에 한 번꼴). 이제 창에 포커스가 있으면 움직임과 마우스·키보드 조작은 모니터 주사율 그대로 그리고, 조용할 때는 60fps, 다른 창 뒤에 있거나 미니 모드일 때만 줄인다.
- **버섯구름의 네모가 중간 크기로.** 덩어리감을 줄이느라 모래알처럼 잘아졌던 구름의 네모를 처음과 지금의 중간 크기로 키웠다. 구름의 빽빽함은 그대로이고, 네모 수는 1,200개에서 220개로 줄어 그리기도 가볍다.

## 0.1.19 — 2026-09-23

- **`책상 펼치기` 가 `햄스터 GUI` 로 — 끄면 핵폭발, 켜면 공사.** `⋯ › 햄스터 GUI`(Ctrl+`)를 끄면 스튜디오가 바로 접히지 않고 4초짜리 연출이 먼저 논다 — 섬광, 사무실 한가운데의 화구, 데크를 가로질러 바다까지 달리는 충격파(지나간 자리의 섬과 사무실이 무너져 내린다), 카메라 흔들림, 그리고 방 안의 햄스터는 전원 재가 되어 하늘로 날아간다. 연출 동안 카메라는 폭발 중심을 보던 것보다 조금 넓게 잡고(공사가 끝나 갈 때 원래 시점으로), 마지막 0.5초에 패널이 미끄러지듯 줄어든 뒤에야 접힌다. 다시 켜면 패널이 자라나며 5초 동안 공사판 — 섬과 사무실이 가운데부터 타일 단위로 바다에서 솟아오르고(바닥 먼저, 책상·벽은 그 위로 자라며 먼지가 인다), 햄스터들은 안전모와 망치 차림으로 하늘에서 떨어져 제자리에서 망치질하다가 공사가 끝나면 모자를 벗고 자리에 앉는다. 전부 연출이라 세션·말풍선 로그·좌석·카메라에는 아무 영향이 없고, 폭발 중에 다시 켜면 곧바로 공사로 넘어간다. 켤 때 처음 뜨는 스튜디오는 공사 없이 그냥 뜨고, OS 의 `동작 줄이기` 설정에서는 예전처럼 바로 접히고 펴진다.
- **스튜디오 마우스 조작이 바뀌었다.** 좌클릭은 상호작용만(햄스터 잡아 던지기, 액자 누르기, 더블클릭으로 자리 보기) — 더는 화면을 끌지 않는다. 둘러보기는 **휠클릭(가운데 버튼) 드래그**, 회전은 우클릭 드래그, 확대는 휠 그대로. 아래 도움말도 `좌클릭 상호작용 · 우클릭 회전 · 휠 확대 · 휠클릭 이동`.
- **화면 전체가 언어 설정을 따른다.** `⋯ › 언어` 가 말풍선 문구만 바꾸고 나머지 UI 는 전부 한국어였다. 이제 메뉴·버튼·툴팁·사이드바·실행 메뉴·세션 바·계정 창·업데이트 안내·알림 창은 물론, 메인 프로세스가 만드는 문구(폴더 대화상자, 5시간 창 오류, 자동 업데이트 콘솔, `CLAUDE.md` 의 멀티 에이전트 블록)까지 고른 언어로 나온다 — 한국어 · English · 日本語 · 中文 · Español · Deutsch · Français · Português · Русский. 바꾸면 그 자리에서 바뀐다. 사전은 `shared/i18n/` 에 언어마다 한 파일이고 한국어가 기준 타입이라 키가 빠지면 빌드가 실패한다. 영어처럼 긴 언어에서 줄이 밀리지 않게 세션 바 알약·계정 행의 사용량 줄·지난 대화의 `--continue` 행·컨텍스트 메뉴 폭을 손봤다.
- **같은 로그인이면 계정이 하나로.** `~/.claude`(CLI 자체 계정)와 앱에서 추가한 계정이 같은 이메일로 로그인돼 있으면 목록에 두 줄로 나오고 CLI 쪽은 `목록에서 빼기` 만 되던 것을, **추가한 계정 하나만** 보이게 했다(지울 수 있는 쪽). `~/.claude` 로 도는 세션·사용량·탭 배지는 그 계정 것으로 세고, 목록 아래 한 줄이 이를 알려 준다. 추가한 계정을 지우면 CLI 계정이 저절로 돌아온다.
- **미니 모드에서 돌아와도 계정 창이 다시 뜨지 않는다.** 켤 때 한 번 답하면 그 실행 동안은 끝인데, 미니 모드를 오가며 메인 화면이 다시 마운트될 때마다 `어느 계정으로 시작할까요?` 가 또 떴다.
- **멀티 에이전트: 동시 에이전트 수 대신 서브에이전트 모델·노력 수준.** `동시 에이전트`(제한 없음 · 2 · 3 · 4 · 6)를 빼고, **서브에이전트 모델**(`메인과 같게` · `한 단계 아래` · Fable · Opus · Sonnet · Haiku)과 **노력 수준**(`같게` · low … max)을 고른다. 고른 값은 `CLAUDE.md` 블록에 Agent 도구의 `model`/`effort` 옵션으로 적히고, `한 단계 아래` 는 매핑(fable→opus, opus→sonnet, sonnet→haiku)을 풀어 쓴다. 예전 설정 파일의 `cap` 은 조용히 무시된다.
- **붙여넣기가 한 번만 된다.** `Ctrl+V` · `Ctrl+Shift+V` · `Shift+Insert` 가 같은 글을 두 번 붙여넣던 것을 고쳤다. 붙여넣는 글은 Windows Terminal 처럼 먼저 걸러서 탭·줄바꿈 말고는 제어 문자(ESC 포함)를 지운다 — 클립보드에 숨은 문자열이 붙여넣기를 일찍 끝내고 명령을 실행하는 일이 없다.
- **파일을 터미널에 끌어다 놓으면 경로가 들어가고, 링크는 `Ctrl+클릭` 으로 연다.** 여러 개면 빈칸으로 잇고, 빈칸이 든 경로는 큰따옴표로 감싼다. 사이드바나 책상에 놓으면 아무 일도 없다 — 예전에는 창 전체가 그 파일로 넘어가 앱이 사라지고 돌아올 길이 없었다. 프로그램이 출력한 하이퍼링크(OSC 8)는 올리면 주소가 보이고 http·https 만 브라우저로 열린다(예전에는 영어 확인 창 뒤에 링크 대신 빈 페이지가 열렸다).
- **권한 선택지를 방향키로 골라도 대기 표시가 풀리지 않는다.** `↑` `↓` 만 눌러도 대기 표시가 꺼지고 세션 바가 다시 켜져서, 그때 누른 `/compact` 의 Enter 가 선택지를 확정해 버릴 수 있었다. 이제 Enter · `Esc` · `Ctrl+C` 로 답했을 때만 풀린다.
- **세션 바가 좁아지면 접힌다.** 책상을 옆에 두거나 사이드바를 열면 `/compact` · `/clear` · `지난 대화` 가 넘쳤다는 표시도 없이 화면 밖으로 밀려났다. 이제 터미널 칸의 폭에 따라 effort 는 드롭다운(`high ▾`)으로, 멀티 에이전트는 체크 상자로, 세 버튼은 `⋯` 하나로 접힌다.
- **탭이 많아도 `+` 가 보이고, 답을 기다리는 탭이 한눈에 보인다.** 탭 줄이 넘치면 휠로 옆으로 넘기고 가려진 끝은 흐려지며, `Ctrl+1~9` 로 간 탭은 화면 안으로 스크롤된다. 권한 요청·질문에 답을 기다리는 탭은 빨간 점 대신 `!` 배지와 호박색 탭으로 바뀌고, 무엇을 기다리는지 툴팁이 말한다.
- **claude 가 없는 탭에도 시작 줄이 있다.** 셸만 있는 탭 위에 `▶ claude` · `지난 대화` · `이어서` 가 세션 바와 같은 줄로 나온다. 햄스터 GUI 를 접어 두어도 지난 대화를 이어 열 수 있고, claude 를 켜고 끌 때 터미널이 위아래로 흔들리지 않는다.
- **책상이 터미널을 가리지 않는다.** 책상을 옆에 두면 터미널 폭 360px, 위에 두면 높이 180px 을 늘 남기고 스플리터도 거기서 멈춘다. 예전에는 좁은 창에서 기본 크기만으로도 터미널이 사라졌다.
- **사이드바: 검색 칸은 탐색 안으로, 누를 수 있는 버튼이 주황.** 맨 위의 `이 폴더에서 검색` 은 탐색 목록만 거르면서 사이드바 전체를 찾는 칸처럼 보여서 탐색 섹션 안, 목록 바로 위로 옮겼다. claude 가 실행 중이라 `터미널 이동` 을 쓸 수 없을 때는 `터미널 새 탭` 이 주황 버튼이 되고 이유가 버튼 아래에 보이며, 좁은 사이드바에서는 버튼이 두 줄로 놓여 번역된 글자가 잘리지 않는다.
- **키보드로 다 된다.** 최근 프로젝트와 탐색 목록은 `↑` `↓` · `Enter` 로 열고(검색 칸의 `Enter` 는 첫 결과), 파일 메뉴는 `Shift+F10` 이나 메뉴 키로 연다. 팝오버는 열면 안으로 포커스가 들어가고 닫으면 연 버튼으로 돌아오며, 켤 때의 계정 창과 업데이트 안내에서는 `Tab` 이 창 밖으로 새지 않는다. 탭 줄은 `←` `→` 로 옮긴다.
- **알림 카드가 종류를 말한다.** 권한 요청은 빨간 띠와 자물쇠, 질문은 호박색 띠와 물음표, 턴 완료는 초록 띠와 체크. 예전에는 카드 전체를 칠해서 권한과 질문이 같은 색이었고, 턴 완료는 주황이라 가장 경고처럼 보였다. 카드 오른쪽의 `›` 가 누르면 그 탭으로 간다는 표시다.
- **퇴근하는 햄스터는 문까지 걸어 나간다.** 먼 자리의 햄스터가 문에 닿기 전에 방 한가운데서 사라지던 것을 고쳤다. 직원은 의자 뒤 통로로 걸어서 의자·동료·정수기를 뚫지 않고, 13번째부터는 문 옆 정수기 앞에 줄을 섰다가 자리가 나면 걸어간다(예전에는 보이지 않다가 의자 위에 나타났다).
- **말풍선이 겹치지 않고, 누가 한 말인지 보인다.** 말풍선끼리 겹치면 서로 비켜 서고, 밀려난 말풍선은 가는 선으로 제 햄스터를 가리킨다. 명패가 보이지 않는 배율에서는 말풍선 앞에 넥타이 색 점과 이름이 붙고, 책상을 옆에 둔 좁은 배율에서도 한 줄은 보인다.
- **햄스터에 올리면 누구인지 카드가 뜬다.** 이름, 에이전트 종류, 모델·effort, 지금 하는 일과 경과 시간, 마지막으로 한 말. 짧게 누르면 카드가 고정되고(`말풍선 로그에서 보기`), 끌면 예전처럼 잡아서 던진다. 트랙패드는 핀치로 확대, 두 손가락 스와이프나 `Shift+휠` 로 이동하고, `Tab` 으로 스튜디오에 포커스를 주면 방향키 · `+`/`−` · `Home` 으로 다룬다.
- **덜 그리고, 다른 터미널을 멈추지 않는다.** 스튜디오는 움직임이 있을 때만 60fps 로 그리고, 조용하거나 창이 뒤에 있거나 미니 모드일 때는 속도를 낮추며 포커스를 잃으면 가벼운 화질로 바꾼다. 앱 밖에서 claude 가 돌고 있으면 몇 초마다 PowerShell 로 프로세스 목록을 다시 묻던 것을 멈췄고, 탭 하나를 닫거나 끊긴 네트워크 드라이브를 훑는 동안 다른 터미널의 출력이 멈추지 않는다.
- **설정 파일을 덮어쓰지 않는다.** 사용량 연동은 `settings.json` 을 읽다 실패하면(깨진 JSON, UTF-8 BOM, 다른 프로그램이 잡고 있는 파일) 빈 설정으로 보고 덮어써서 permissions · hooks 가 사라질 수 있었다. 이제 BOM 은 벗겨 읽고, 그래도 못 읽으면 아무것도 쓰지 않은 채 이유를 보여 주며, 쓸 때는 `settings.json.hamster-bak` 백업을 남긴다. npm 으로 설치한 claude(`claude.cmd`)에서 늘 실패하던 말풍선 요약도 이제 되고, 화면을 그리는 프로세스가 죽으면 흰 창으로 남지 않고 다시 읽힌다.
- **날짜는 그 언어의 모양으로, `동작 줄이기` 도 따른다.** 최근 목록과 사용량 초기화 시각의 `9/12` 가 언어에 맞게 `9월 12일` · `Sep 12` · `12. Sept.` 로 나오고, `1 summaries` 같은 복수형을 고쳤다. Windows 의 `애니메이션 효과` 를 끄면 팝오버·턴 카드처럼 화면 곳곳의 움직임도 곧바로 끝난다.

## 0.1.18 — 2026-09-22

- **사이드바의 `여기서 터미널 열기` 가 `터미널 이동` · `터미널 새 탭` 둘로.** `터미널 이동`(주황)은 앞에 있는 터미널에 `Set-Location -LiteralPath '<폴더>'` 를 쳐서 그 폴더로 옮기고, 탭 이름·git 칩·탐색 위치·다음 실행 때 복원되는 폴더가 같이 따라간다. 터미널이 없거나 그 탭에 claude 가 실행 중이면 비활성이고 툴팁이 이유를 말한다(claude 가 끝나면 다시 된다). `터미널 새 탭` 은 예전 그대로 그 폴더의 터미널을 새 탭으로.
- **켤 때 계정 창이 로그인 전에도 뜬다.** 하나뿐인 계정이 아직 로그인 전이면 `먼저 로그인할까요?` 가 떠서 `로그인`(그 계정의 터미널에서 브라우저 로그인 → `claude`) 또는 `계정 추가`(이름 → 추가 후 로그인)로 시작할 수 있고, 행을 누르면 그냥 시작한다. 계정이 여럿일 때도 로그인 전 계정에는 `로그인` 버튼이 붙는다.
- **계정 창과 업데이트 안내가 화면 한가운데.** 둘 다 위에서 12vh 자리에 붙어 위로 치우쳐 보였다. 계정 창의 행은 테두리 상자 대신 `⋯ › 계정` 목록과 같은 행 모양으로.

## 0.1.17 — 2026-09-22

- **햄스터를 잡아서 던진다.** 책상의 햄스터를 왼쪽 버튼으로 잡아 끌면 손에 매달려 네 발을 허우적대고(💦), 놓으면 손 속도대로 날아간다 — 세게 던지면 공중제비. 바닥에 떨어지면 알아서 자리로 걸어가고(책상 위에 떨어지면 가장자리로 밀려나서, 밖이면 문으로 돌아 들어와서), **바다에 떨어지면** 물보라와 함께 가라앉고 털색이 한 단계 다른 새 햄스터가 문으로 걸어 들어와 같은 자리에 앉는다. 전부 연출이라 세션·말풍선 로그·좌석에는 아무 영향이 없다. 잡은 사장은 순찰을 그만두고, 드는 동안 자동 카메라는 멈추며, 던진 뒤에는 카메라가 따라가 물보라까지 보인다.
- **켤 때 계정을 묻는다.** 계정이 둘 이상이면 로딩 뒤 `어느 계정으로 시작할까요?` 가 떠서 하나를 골라야 넘어간다(지난번 계정에 표시와 포커스, Enter 로 그대로). 돌아온 탭은 각자 열 때의 계정 그대로다.
- **설정에서 계정을 누르면 그 계정의 새 터미널이 바로 열린다.** `⋯ › 계정` 의 행이 활성 계정만 바꾸고 끝나서 `+` 를 따로 찾아야 했다. 이제 앞 탭의 폴더에서 그 계정의 탭이 열리고 메뉴는 닫힌다.
- **시작 카드의 `이어서 실행 --continue` 가 `지난 대화` 로.** 세션 없는 터미널 위의 카드에 세션 바와 같은 지난 대화 목록(검색·브랜치·실행 중 배지·맨 아래 `--continue`)이 열리고, 고른 대화는 새 탭이 아니라 **그 터미널에서** `claude --resume` 으로 이어진다.
- **멀티 에이전트 방식 메뉴가 넓어졌다.** 스타일시트의 320px 상한이 지정 폭을 덮어써서 `계획 → 분담 → 검토` 줄의 설명이 두 줄로 밀려 다음 행과 겹쳤다. 팝오버는 지정 폭이 이기고, 이 메뉴는 400px. 좁은 창에서 줄이 넘치면 행이 겹치는 대신 늘어난다.
- **실행 메뉴의 머리줄과 행이 구분된다.** `개발` 머리줄과 그 아래 `개발 서버` 행이 같은 목록의 두 줄로 읽혔다. 머리줄은 작고 흐린 굵은 글씨에 앞에 주황 눈금, 그룹 사이 가는 줄, 행은 들여쓰기.
- **사장 정장이 등을 덮는다.** 등판이 몸 뒤에 떠 있는 얇은 판이라 뒤쪽 모서리에 털이 보였고, 순찰하러 걸어가는 사장을 뒤에서 보면 옆판만 있는 것처럼 보였다. 옆판을 등까지 잇고 등판을 몸에 붙는 두꺼운 판으로 바꿔 한 벌로 둘렀다.

## 0.1.16 — 2026-09-22

- **모델별 주간 사용량.** Max 플랜은 모든 모델을 합친 주간 창 말고 **Fable 만의 주간 창**이 따로 있어서 그쪽이 먼저 차는데, 상단 게이지는 전체만 보여 줘서 알 수 없었다. 이제 앱이 로그인된 계정마다 CLI 에 직접 물어(모델 호출 없음, 토큰 0) 모델별 창을 받아 온다 — 켤 때와 10분마다, 팝오버의 `다시 확인` 으로도. 게이지 아랫줄은 주간 창들 중 **가장 찬 것**(`주 74% Fable`)이고, 누르면 `주간 (전체)` · `주간 (Fable)` 이 다 나온다.
- **탐색에 `▶ 실행 ▾` 메뉴.** 폴더가 Node·Python·Rust·Go·.NET·Maven·Gradle·Ruby·Makefile·Docker Compose 프로젝트면 `여기서 터미널 열기` 아래에 알약이 생긴다(`실행 · Node · pnpm · Vite`). 개발 서버·빌드·테스트·설치 명령을 고르면 그 폴더의 새 탭에서 바로 실행된다 — `package.json` 의 스크립트와 잠금 파일로 npm/pnpm/yarn/bun 을 고르고, `.venv` 가 있으면 먼저 활성화한다. 아무것도 실행하지 않고 폴더 안 파일만 읽어서 알아낸다.
- **알림이 진짜로 뜬다.** Windows 토스트가 **방해 금지**에 삼켜져 작업 표시줄만 깜빡이고 아무것도 안 보이던 것(선택지 질문 때 특히)을, OS 설정과 무관하게 앱이 직접 그리는 **알림 창**으로 바꿨다 — 오른쪽 아래, 최대 3장, 포커스를 뺏지 않고, 마우스를 올리면 멈추고, 누르면 그 탭으로. 질문·권한 요청은 15초, 턴 완료는 8초.
- **강조색이 클로드 주황.** 초록이던 버튼·선택·포커스·작업 중 점·터미널 커서·검색 일치를 테라코타 주황으로(라이트 `#b04f2c` · 다크 `#e0855f`). 사용량·컨텍스트 게이지의 "괜찮음" 칸과 `+N` 은 상태색 초록으로 남긴다.
- **사장이 순찰한다.** 서브에이전트가 일하는 동안 사장 햄스터가 가끔 일어나 직원에게 걸어가 혼내고(💢·💦, 말풍선 없음) 돌아온다. 순전히 연출이라 상태·말풍선 로그·좌석에는 영향이 없고, 권한 요청이나 실제 발화가 오면 바로 자리로 돌아온다.
- **Spritfy 액자 하나 더, 올리면 버튼처럼.** 서쪽 벽(문과 포스터 사이)에 1.4배 크기로 하나 더 걸어서 동료가 있는 넓은 구도에서는 화면 왼쪽에 크게 보인다. 두 액자 모두 마우스를 올리면 살짝 떠오르며 제 색으로 빛나고 아래에 `spritfy.xyz 열기 ↗` 캡션이 붙는다. 누르면 브라우저로.
- **왕관 대신 정장.** 왕관은 Fable 스킨의 장식이라 Fable 이 기본 모델이 된 뒤로 방 안 전원이 쓰고 있었다. 이제 사장(메인)만 짙은 정장 재킷을 입고, Fable 의 머리 장식은 베레모다.
- **멀티 에이전트** (세션 컨트롤 바, 기본 켜짐): 작업을 서브에이전트에게 나눠 맡기라는 지시를 그 계정의 `CLAUDE.md` 끝에 표시된 블록으로 넣어 둔다 — 프롬프트마다 붙이는 게 아니라 하네스처럼 상시 지시로. 방식은 `필요할 때만` · `적극 분담` · `계획 → 분담 → 검토` · `직접 입력`, 동시 에이전트 상한도 고른다. 끄면 블록만 사라지고 나머지는 그대로. 다음에 여는 `claude` 부터 적용된다.

## 0.1.15 — 2026-09-22

- **터미널 탭이 두 줄.** 윗줄은 대화 제목 하나뿐이고, 아랫줄에 폴더 이름 · 계정 · 컨텍스트 % · git · 🐹 수가 온다. 한 줄에 다 붙어서 제목이 `예약메시지 및 5시…` 로 짓눌리던 것을 고쳤다. 아랫줄에 쓸 것이 없으면 제목만 가운데에 온다.
- 우상단의 `바뀐 파일 N` 칩을 뺐다 — 사이드바의 `바뀐 파일` 섹션과 같은 것이라 자리만 차지했다.

## 0.1.14 — 2026-09-22

- **즐겨찾기·최근 프로젝트가 사라지던 문제.** 설치판과 개발·캡처 실행이 같은 `ui.json` 을 각자 메모리에 든 사본으로 통째로 덮어써서, 다른 쪽이 더한 즐겨찾기·최근이 지워졌다(마지막에 쓴 쪽이 이긴다). 이제 쓰기 전에 파일을 다시 읽고 바뀐 키·목록의 차이만 얹는다. 캡처 실행은 설정 파일에 아무것도 쓰지 않는다. 시작 카드가 켤 때마다 "아직 연 프로젝트가 없어요" 로 비어 보이던 것도 고쳤다(파일을 읽기 전에 그렸다). 이미 사라진 항목은 되살릴 수 없다.
- **탐색이 앞에 있는 터미널의 폴더를 따라간다.** 폴더 찾아보기·최근·즐겨찾기로 열거나 탭을 바꾸면 그 폴더로 가고, 탭이 없으면 마지막 폴더 → 홈. 앱의 설치 폴더를 보여 주던 문제를 고쳤다(처음 한 번, 빈 경로로 목록을 만들고 있었다).
- **세션 바에서 모델을 바꾼다.** 모델 이름(`Fable 5.1 ▾`)을 누르면 드롭다운이 열리고, `fable · opus · sonnet · haiku` 중 하나를 고르면 effort 와 똑같이 `/model <alias>` 를 쳐 준다. Claude Code 가 그 값을 새 세션의 기본 모델로도 저장한다는 점은 메뉴에 적혀 있다.
- **상단 사용량 게이지가 두 줄.** 5시간·주간 칩 둘을 칩 하나에 위아래로 쌓아 폭을 절반으로 줄였다(1280px 창에서 328px → 166px). 바 높이는 그대로, 좁은 창에서는 남은 시간 → 세그먼트 순으로 접히고 주간 줄은 늘 남는다.
- **사무실 벽에 Spritfy 액자.** 사장 자리 동쪽 벽감에 로고와 `3D & 스프라이트 생성 툴 · spritfy.xyz` 가 걸린다. 누르면 기본 브라우저에서 spritfy.xyz 가 열린다.

## 0.1.13 — 2026-09-22

- **앱 업데이트는 켤 때만 확인한다.** 한 시간마다 하던 확인을 뺐다 — 작업 중간에 재시작을 묻고, 다운로드가 돌아가는 Claude 밑에서 디스크를 잡았다. 새 버전은 다음에 앱을 켤 때 잡히고, 바로 보고 싶으면 `⋯` 메뉴의 `다시 확인`.

## 0.1.12 — 2026-09-22

- **선택지형 질문에도 알림이 뜬다.** Claude 가 선택지로 물어볼 때(AskUserQuestion) 창이 뒤에 있어도 아무 알림이 없었다 — CLI 2.1.x 의 질문 화면 문구가 바뀌어서 앱이 알아보지 못했다. 이제 화면과 대화 기록 두 곳에서 질문을 알아보고, 창이 포커스 밖이면 오른쪽 아래 Windows 알림과 작업 표시줄 깜빡임으로 알린다.
- **5시간 창 자동 시작**(`⋯ > 5시간 창 자동 시작`, 계정별, 기본 꺼짐): 5시간 한도가 초기화되면 1분 뒤 그 계정으로 Haiku 에게 한 단어를 보내 다음 5시간을 바로 시작한다. 창이 끊김 없이 이어져서, 오랜만에 돌아와도 이미 돌고 있는 창에서 시작한다. 한 번에 토큰 400개쯤, 앱이 켜져 있을 때만 동작한다. 다음 시작 시각과 실패 이유는 메뉴의 계정 아래에 나온다.
- **말풍선 요약은 기본으로 꺼져 있다.** 요약 한 번이 구독으로 Haiku 를 한 번 부르는 것이라, 원할 때 `⋯ > 말풍선 > 요약해서 말하기` 로 켠다. 지금까지 켜져 있던 설정도 이번에 한 번 꺼진다(옛 기본값이었을 뿐이라). 켜면 요약이 한 번에 3초쯤 더 빨리 온다.
- **앱 업데이트는 묻기 전에 받지 않는다.** 새 버전이 있으면 알리기만 하고, `업데이트 받기` 를 눌러야 받기 시작한다. `나중에` 는 정말 나중이다. 이 동작은 0.1.12 부터라, 0.1.11 에서 이번 버전으로 올라올 때는 예전처럼 미리 받아 둔다.

## 0.1.11 — 2026-09-22

- **GitHub 의 릴리스 목록이 잠깐 죽어도 새 버전을 찾는다.** 0.1.10 이 나온 밤에 GitHub 의 릴리스 피드가 504 를 돌려줘서, 파일은 멀쩡히 받을 수 있는데도 설치된 앱이 `확인 실패`로 새 버전을 보지 못했다. 이제 그 피드가 실패하면 최신 릴리스의 파일에 직접 물어본다(이 경로로는 바뀐 부분만이 아니라 설치 파일 전체를 받는다).
- 업데이트 기록(`~/.hamster-desk/update.log`)에 같은 오류가 두 번씩 찍히던 것을 고쳤다.

## 0.1.10 — 2026-09-22

- **Ctrl+W 로 창이 닫히지 않는다.** 터미널과 Claude 입력줄에서 Ctrl+W 는 "앞 단어 지우기"인데 창이 통째로 닫혔다. Ctrl+R(기록 검색)이 화면을 새로고침해 터미널을 전부 날리던 것, Ctrl+± 가 글꼴과 함께 화면 배율까지 바꾸던 것도 같이 고쳤다. 탭 닫기는 그대로 Ctrl+Shift+W.
- **시작할 때 아무 터미널도 열지 않는다.** 최근 프로젝트·활성 계정·폴더 찾아보기가 있는 시작 카드로 뜨고, 고르면 그때 열린다. 지난 탭을 되살리고 싶으면 `⋯ > 시작할 때 지난 탭 다시 열기`.
- **계정 목록에 사용량**: 계정 메뉴의 각 계정 아래에 그 계정이 마지막으로 알려 준 사용량이 나온다(`5h 81% · 주 42% · 3분 전`).
- 앱 안에서 방금 켠 Claude 가 잠깐 "다른 터미널의 세션" 탭으로 하나 더 뜨던 것을 고쳤다.

## 0.1.9 — 2026-09-21

- **설치한 앱의 자동 업데이트가 실제로 동작한다.** 0.1.1~0.1.8 의 설치판은 업데이트 확인이 매번 `확인 실패`(`Cannot set properties of undefined (setting 'autoDownload')`)로 끝났다 — 업데이트 모듈을 불러오는 방식의 버그였다. **그 버전들을 설치한 PC 는 이번 설치 파일을 한 번 직접 받아 설치해야 한다.** 그다음부터는 켜자마자 팝업이 뜨고 스스로 업데이트한다.
- 소스에서 빌드하는 경우: 이제 `npm install` 에 `--legacy-peer-deps` 를 붙이지 않아도 된다(저장소의 `.npmrc` 가 같은 설정을 준다). 옵션을 빼먹으면 `package-lock.json` 이 바뀌던 문제가 없어진다.

## 0.1.8 — 2026-09-21

- 버그 수정. 앱의 동작은 0.1.7 과 같다(업데이트 과정을 시험하기 위한 릴리스).

## 0.1.7 — 2026-09-21

- **맨 위 계정도 목록에서 뺄 수 있다**: CLI 가 원래 쓰던 계정(`~/.claude`)에도 `×` 가 생겼다. 지우지는 않는다 — 로그인과 폴더는 그대로 두고 목록에서만 빼며, 그 계정으로 열린 탭만 닫는다. 계정 메뉴의 `CLI 계정(~/.claude) 다시 표시` 로 되돌린다. 마지막 남은 계정은 뺄 수 없다.
- 사이드바의 빈 `바뀐 파일`·`말풍선 로그` 에서 안내 문구를 뺐다. 흐린 머리줄에 이름과 0 만 나온다.

## 0.1.6 — 2026-09-21

- **켜자마자 업데이트 팝업**: 새 버전이 확인되면 화면 가운데에 "새 버전이 나왔어요" 창이 뜬다. 받는 동안은 진행 막대가, 다 받으면 `다시 시작해서 업데이트` 버튼이 켜진다. `나중에` 를 누르면 상단의 `앱 업데이트` 버튼만 남고 다음에 켤 때 다시 묻는다.
- **업데이트 중 "경로가 존재하지 않습니다"**: 설치는 실행 파일을 30초쯤 없애는데 지금까지는 아무 표시 없이 돌아서, 그사이 작업 표시줄 아이콘을 누르면 저 오류가 났다(설치가 깨지기도 했다). 이제 설치 창이 보이는 채로 진행되고 앱이 스스로 다시 열린다. 앱을 끌 때 몰래 설치하던 것은 껐다.
- **"확인 실패"에 이유가 보인다**: `⋯` 메뉴의 확인 실패 아래에 이유가 나오고, 20초·1분·3분 뒤에 스스로 다시 확인한다. 확인·다운로드·설치·오류는 `~/.hamster-desk/update.log` 에 남는다.
- 소스에서 빌드한 앱은 업데이트 확인을 git 으로 한다(GitHub API 의 시간당 호출 한도에 걸리지 않는다). `닫고 업데이트` 는 `npm install` 이 바꿔 놓은 `package-lock.json` 때문에 멈추지 않고, 창 첫 줄에 "끝나면 스스로 다시 열린다"고 알린다.
- 작업 표시줄: 예전 포터블이 임시 폴더에 남긴 실행 파일을 가리키는 바로가기도 고친다.

## 0.1.5 — 2026-09-21

- **계정**: "기본 계정"이 없어졌다. 모든 계정이 똑같이 등록되고 그중 하나가 **활성**(새 터미널이 열리는 계정)이다. CLI 가 원래 쓰던 계정도 목록의 한 계정으로, 이름을 짓기 전에는 로그인 이메일의 앞부분으로 보이고 같은 `로그인` 버튼을 쓴다.
- **말풍선을 누르면 상세 내역**: 스튜디오의 말풍선을 누르면 사이드바의 말풍선 로그가 열리고 그 줄이 펼쳐진다. 로그의 줄도 누르면 그 자리에서 펼쳐져 요약 전 원문 전체와 누가·언제·몇 번이 보이고, `햄스터 보기`·`복사` 버튼이 있다. 말풍선 닫기는 우클릭으로 옮겼다.
- **빈 섹션도 보인다**: `바뀐 파일`·`말풍선 로그`가 내용이 없어도 사이드바에 흐린 한 줄로 나와서, 써 보기 전에도 어디에 생기는지 알 수 있다.
- **사이드바 높이 조절**: `바뀐 파일`·`말풍선 로그`의 아래쪽 가장자리를 끌어 높이를 바꾼다(탐색이 나머지를 가진다). 더블클릭하면 자동 배치로 돌아가고, 높이는 저장된다.
- GitHub 첫 화면(README)과 저장소 소개란에 설치 프로그램 내려받기 링크를 달았다.

## 0.1.4 — 2026-09-21

- 빌드: `npm run build` 하나로 설치 프로그램(`Hamster-Desk-Setup-<버전>.exe`)과 압축 없는 폴더가 함께 만들어진다. 전에는 설치 프로그램을 받으려면 `npm run dist` 를 따로 돌려야 했다. 폴더만 빠르게 필요하면 `npm run build:dir`.
- "닫고 업데이트"가 폴더만 다시 빌드해서 더 빨리 끝난다. 앱 자체의 동작은 0.1.3 과 같다.

## 0.1.3 — 2026-09-21

- 버그 수정 및 배포 자동화: 이제 누가 어느 PC 에서 올리든 새 버전이 자동으로 빌드되어 배포된다. 앱 자체의 동작은 0.1.2 와 같다.

## 0.1.2 — 2026-09-21

- 버그 수정: 새 버전을 게시할 때 설치 파일이 빠진 채 잠깐 공개될 수 있던 문제를 고쳤다(0.1.1 을 올릴 때 실제로 일어났다). 앱 자체의 동작은 0.1.1 과 같다.

## 0.1.1 — 2026-09-21

- **설치 프로그램**: git·node 없이 쓸 수 있는 `Hamster-Desk-Setup-<버전>.exe`. 관리자 권한 없이 설치되고, 시작 메뉴·바탕 화면 바로가기가 생긴다. 이렇게 설치하면 작업 표시줄에 어떻게 고정해도 버튼이 하나다.
- **자동 업데이트**: 설치한 앱은 새 릴리스를 백그라운드로 받아 두고, 상단의 "앱 업데이트 → 다시 시작해서 업데이트"를 누르거나 앱을 끌 때 스스로 새 버전이 된다. 다시 설치할 필요가 없다.
- **계정이 사라지던 문제**: 재부팅 직후처럼 설정 파일을 잠깐 읽지 못한 순간에 앱이 빈 설정으로 파일 전체를 덮어써서 계정 목록·탭·최근 폴더가 한꺼번에 날아갈 수 있었다. 이제 못 읽은 파일은 절대 덮어쓰지 않고, 저장은 디스크에 확실히 기록하며, 파일이 깨지면 직전 백업에서 복구한다.
- **계정 되찾기**: 목록에서는 사라졌지만 로그인이 남아 있는 계정 폴더를 시작할 때 자동으로 목록에 되돌린다. 다시 로그인할 필요가 없다.
- **작업 표시줄 고정**: 실행 중인 창을 고정했을 때 눌러도 켜지지 않던 문제를 막았다. 창이 자신의 실행 파일·아이콘·ID 를 직접 알려 준다.
- `⋯` 메뉴의 Hamster Desk 줄에 앱 버전이 함께 보인다.
